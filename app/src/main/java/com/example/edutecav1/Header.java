package com.example.edutecav1;

import android.os.Bundle;
import android.widget.TextView;

import androidx.appcompat.app.AppCompatActivity;

import java.util.HashMap;

public class Header extends AppCompatActivity {

    /*
    private TextView txtNomeLogado;
    SessionUser sessionUser;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_header);

        sessionUser = new SessionUser(this);
        sessionUser.checkLogin();

        txtNomeLogado = findViewById(R.id.txtNomeLogado);

        HashMap<String, String> user = sessionUser.getUser();
        String lNome = user.get(sessionUser.NOME);

        txtNomeLogado.setText(lNome);
    }*/
}
